{
  "buildCommand": "pnpm build",
  "outputDirectory": ".next",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD .",
  "framework": "nextjs",
  "crons": [{
    "path": "/api/cron",
    "schedule": "0 10 * * *"
  }]
}
